<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SAF pickers don't strictly require these, but some OEM file pickers
         and external player apps behave inconsistently without the app
         also holding read access - requested at runtime before Import/Link
         open a picker, see LibraryScreen/LinkScreen. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".MofyApplication"
        android:label="Mofy"
        android:allowBackup="false"
        android:theme="@style/Theme.Mofy">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Mofy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
