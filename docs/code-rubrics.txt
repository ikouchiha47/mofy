Still open toward your bigger rubric goal (SOLID, code coverage, SonarQube, architecture): this is only the integration confirmation, not the rubric design itself. When you're ready for that conversation, worth deciding upfront: what layer boundaries actually exist to enforce (currently no module split — everything's one Gradle module), what coverage tool (Jacoco is the standard Android pairing), and whether SonarQube runs locally/CI or is out of scope for a personal app. Happy to scope that as its own piece whenever you want to tackle it.
