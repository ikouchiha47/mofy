# Tasks: Phase 00 — Skeleton

See `docs/RALPH.md` for how to run this list, `docs/phases/00-skeleton.md`
for requirements.

- [ ] Initialize Kotlin/Compose Android project with standard Gradle layout
- [ ] Add `.env` (committed, dummy values) and `.env.prod` (gitignored) file
      loading for `TMDB_API_KEY`
- [ ] Add gitignore entry for `.env.prod`
- [ ] Build a minimal Compose navigation shell with placeholder screens
      (Home, Search, Library, Settings)
- [ ] Add a startup check that fails loudly if `TMDB_API_KEY` is missing/empty
</content>
