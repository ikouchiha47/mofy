[project]
name = "mofy-ml"
version = "0.1.0"
description = "Offline catalog-building pipeline for Mofy: IMDb filtering, TMDB enrichment, EmbeddingGemma embeddings"
requires-python = ">=3.11"
dependencies = [
    "polars>=1.9",
    "httpx>=0.27",
    "sentence-transformers>=3.0",
    "tqdm>=4.66",
]

[tool.uv]
package = false
